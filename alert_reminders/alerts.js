function tournamentMsg(msg) {
    var myName = "Joe"
    alert(msg);
}


//variable has to be declared as in var my_name = "{{ session['my_name']}}";
//would it work without the outside double quotes? or not because its python being called in by js?
